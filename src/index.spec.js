import cfnDrift from './index';


describe('Cloudformation Drift Detection', async => {

    it('triggers drift detection with correct args', () => {

    });

    it('it polls for drift results every 2 seconds until DetectionStatus.DETECTION_COMPLETE', () => {

    });

    it('it polls for drift results every 2 seconds until DetectionStatus.DETECTION_FAILED', () => {

    });

    it('returns promise resolve on StackDriftStatus.IN_SYNC', () => {

    });

    it('returns promise reject on StackDriftStatus.DRIFTED', () => {

    });

    it('returns promise reject on StackDriftStatus.NOT_CHECKED', () => {

    });

    it('returns promise reject on StackDriftStatus.UNKNOWN', () => {

    });
});